# ── 密钥 / 环境变量（绝不提交）──
.env
.env.*
# 但 .env.template（变量模板）需要提交供他人参考
!.env.template
*.pem
*.key
secrets/

# ── 依赖 / 构建产物 ──
node_modules/
backend/target/
target/
dist/
build/

# ── 数据导出 / 备份（含个人信息，勿提交）──
*.bak_*
*.xlsx
*.xlsx.bak_*

# ── 日志 / 临时 ──
*.log
logs/
tmp/
tmp_*
.tmp_*
_*.mjs
_*.json
stdout.txt
stderr.txt

# ── IDE / 系统 ──
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# ── Claude Code 会话文件 ──
.waylog/
.agents/
.claude/
.tmp_*

# ── GitHub AI 建议（非项目源码）──
.github/modernize/
